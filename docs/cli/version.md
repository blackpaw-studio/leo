# leo version

Print the Leo version.

## Usage

```bash
leo version
```

## Output

```
leo v0.3.2
```

The version is injected at build time via Go linker flags from git tags. Development builds show `leo dev`.
