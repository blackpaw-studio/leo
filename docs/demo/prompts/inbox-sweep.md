# inbox-sweep (demo prompt)

This file exists only to satisfy leo's prompt_file reference. The task
is never actually executed during the demo — leo just validates that
the file is present and schedulable.
