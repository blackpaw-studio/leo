FROM golang:1.25-bookworm

RUN apt-get update && apt-get install -y tmux cron && rm -rf /var/lib/apt/lists/*

WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .

CMD ["go", "test", "-tags=e2e", "-v", "-count=1", "./e2e/..."]
